import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ls-personal-site',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
